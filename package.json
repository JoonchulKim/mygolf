{
  "name": "mygolf",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js && node scripts/build-sw.mjs --check",
    "start": "python3 -m http.server 8000",
    "prepare:release": "node scripts/build-sw.mjs"
  }
}
